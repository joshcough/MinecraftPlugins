name: MineLangPlugin
main: jcdc.pluginfactory.MineLangPlugin
author: Josh Cough
version: 0.3.1
database: false
depend: [ScalaLibraryPlugin]
commands:
  import:
    description: import some defs
    usage: /import existing-file
  run:
    description: run a program
    usage: /run slurp
  reload-code:
    description: run a program
    usage: /reload-code 