name: Warp
main: jcdc.pluginfactory.WarpPlugin
author: jacycough
version: 0.1
database: true
commands:
   set-warp:
      description: Create a new warp location.
      usage: /<command> warp-name
   warp:
      description: Warp to the given warp location.
      usage: /<command> warp-name
   warps:
      description: List all warps.
      usage: /<command>
   delete-all-warps:
      description: Delete all warps in the database. Admin only!
      usage: /<command>
   delete-warp:
      description: Delete a warp location.
      usage: /<command> warp-name