name: NPC
main: jcdc.pluginfactory.examples.NPC
author: Josh Cough
version: 0.1
database: false
commands:
  spawn:
    description: Spawn an NPC
    usage: /<command> string
  fd:
    description: Move an NPC forward
    usage: /<command> npc  number
  kill-npc:
    description: Kill an NPC
    usage: /<command> npc